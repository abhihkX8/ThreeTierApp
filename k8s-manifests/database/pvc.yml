apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mongo-pvc
  namespace: three-tier

spec: 
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 2Gi



